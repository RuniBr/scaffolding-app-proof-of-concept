import { colors as nothsColors } from '@noths/styles-config';

const checkoutColors = {
  white: '#FFFFFF',
  background: '#F4F6F6',
  alto: '#d0d0d0',
  doveGray: '#686868',
  buttercup: '#f4a929',
};

const colors = {
  ...nothsColors,
  ...checkoutColors,
};

export default colors;
